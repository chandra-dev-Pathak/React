### 1. Component Reusability
- Created a `Card` component and used it multiple times inside `App.jsx`
- Learned how React components help avoid repetition (DRY principle)

### 2. JSX Structure
- Practiced writing HTML-like syntax inside JavaScript
- Used React fragments (`<> </>`) to wrap multiple elements

### 3. CSS Styling in React
- Used external CSS file (`App.css`) to style components
- Applied classes using `className` instead of `class`

### 4. Variable use in JSX

- We inject js variable in html like code(JSX) using curly brackets {}